#include<stdio.h>
#include<math.h>
int main(){
long long max=(long long int)(pow(2,63)-1);
long long min=(long long int)(pow(2,63)*-1);
printf("the higest number is represented unsigned long long by %lld\n",max);
printf("the lowest number is represented unsigned long long by %lld\n",min);
return 0;
}
